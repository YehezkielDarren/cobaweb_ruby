<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/time.css">
  <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/LogoHeader1.png"/>
  <script src="/script.js"></script>
  <script src="/time.js"></script>
</head>
<body>
  <header>
    <div class="logo">
    <img src="/LogoHeader1.png" alt="logokerja" />
    <a>Cari Kerja. <span class="small">com</span></a>
    </div>
  </header>
  <main>
    <div class="wrapper">
      <% if defined?(pesan_gagal) && pesan_gagal && !pesan_gagal.empty? %>
        <%= alert_component(pesan_gagal, "danger") %>
      <% end %>
      <% if defined?(pesan_sukses) && pesan_sukses && !pesan_sukses.empty?%>
        <%= alert_component(pesan_sukses, "success") %>
      <% end %>

      <form action="/login" method="post">
        <h1>Login</h1>

        <%# Memanggil partial untuk input username %>
        <%= erb :_input_field, locals: { type: "text", name: "username", placeholder: "Username", icon_class: "bx-user", required: true } %>

        <%# Memanggil partial untuk input password %>
        <%# Untuk partial _input_field.erb, pastikan icon_class menyertakan 'toggle-password' jika diperlukan %>
        <%# dan tambahkan onclick="showPassword()" jika fungsi tersebut masih relevan untuk ikon di dalam partial %>
        <%= erb :_input_field, locals: { type: "password", name: "password", placeholder: "Password", icon_class: "bx-show toggle-password" } %>
        <%# Jika ikon di partial perlu onclick: <i class='bx <%= icon_class %> <%= defined?(onclick_event) ? "onclick=\"#{onclick_event}\"" : "" %> </i>

        <button type="submit" class="btn">Login</button>
      </form>
    </div>
  </main>
  <div class="clock-container">
    <div class="clock-time" id="clock">00:00:00</div>
    <div class="clock-date" id="date">Loading date...</div>
  </div>
</body>
</html>
